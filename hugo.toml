baseURL = 'https://example.org/'
languageCode = 'en-us'
title = 'DevOps For Project Managers'
theme = "docdock"

[outputs]
home = [ "HTML", "RSS", "JSON"]

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true